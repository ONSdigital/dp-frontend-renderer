---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: onsdigital/dp-concourse-tools-git

inputs:
  - name: build-bundle
  - name: dp-frontend-renderer

outputs:
  - name: build

run:
  path: dp-frontend-renderer/ci/scripts/unpack.sh
