---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: onsdigital/dp-concourse-tools-git

inputs:
  - name: artifacts
  - name: dp-frontend-renderer

outputs:
  - name: build

run:
  path: dp-frontend-renderer/ci/scripts/build-bundle.sh
